{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'no project-wide build needed - api/*.js is pre-bundled by scripts/bundle-vercel-api.mjs before vercel build runs'",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/mcp", "destination": "/api/mcp" },
    { "source": "/", "destination": "/api/health" }
  ]
}
